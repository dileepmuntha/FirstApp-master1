<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firstapp">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".Version"></activity>
        <activity android:name=".PeopleAbout" />
        <activity android:name=".LeaderAbout" />
        <activity android:name=".ViewPosts" />
        <activity android:name=".CreatePost" />
        <activity android:name=".ForgetPassword" />
        <activity android:name=".Feedback" />
        <activity android:name=".AboutApp" />
        <activity android:name=".Profile" />
        <activity android:name=".ChooseArea" />
        <activity android:name=".Village">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PhoneNumber" />
        <activity android:name=".PublicLogin" />
        <activity android:name=".LeaderProfile" />
        <activity android:name=".Leader_regist" />
        <activity android:name=".leaderlogin" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>